'use client';

export default function CoreChatsPage() {
  return (
    <div className="flex h-full min-h-0 flex-1 items-center justify-center bg-surface text-ink dark:bg-[rgb(var(--tg-bg))] dark:text-white">
      <div className="text-sm text-ink-muted">Выберите чат</div>
    </div>
  );
}
